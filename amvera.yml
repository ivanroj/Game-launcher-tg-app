meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10
build:
  requirementsPath: requirements.txt
run:
  persistenceMount: /data
  containerPort: 80
  scriptName: bot.py
  command: gunicorn GameLauncher.wsgi:application  --bind 0.0.0.0:80 && python bot.py